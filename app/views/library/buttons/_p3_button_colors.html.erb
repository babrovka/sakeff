<%= library_item_caption 'Цвета кнопок' %>
<%
   all_states = %w(
                      cyan
                      cyan-d
                      sea-green
                      sea-green-d
                      green
                      green-d
                      blue
                      blue-d
                      magenta
                      magenta-d
                      asphalt
                      asphalt-d
                      yellow
                      yellow-d
                      orange
                      orange-d
                      red
                      red-d
                      cloud
                      cloud-d
                      gray
                      gray-d
                  )
%>
<p>Для придания кнопке того или иного цвета нужно использовать класс <span class="lib-prop">.btn-{имя_цвета}</span></p>
<%= library_example do %>
  <% all_states.each do |state_name| %>
    <a class="btn btn-<%= state_name %>"><%= "btn-#{state_name}" %>&nbsp&nbsp&nbsp&nbsp&nbsp</a>
  <% end %>

<% end %>
<% slim_code = all_states.map do |state_name|
  "a.btn.btn-#{state_name} btn-#{state_name}"
end %>
<%= highlight_named_code slim:  slim_code.join("\n") %>
console.log "created"
# @todo here a new bubble must be added to a node
# @todo here also all parents should get new counts
formContainer = $(".js-bubble-form")
formContainer.modal("hide")
formContainer.find("form")[0].reset()